<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Biometric unlock for the optional App Lock (V3-M4). local_auth's
         BiometricPrompt requires USE_BIOMETRIC; androidx.biometric handles the
         fingerprint fallback on older APIs, so no separate USE_FINGERPRINT. -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <!-- Local reminder notifications (V3-M5). POST_NOTIFICATIONS is the Android
         13+ runtime prompt (requested in-context on first toggle, not at boot).
         RECEIVE_BOOT_COMPLETED lets ScheduledNotificationBootReceiver re-arm the
         daily nudge after a reboot. NO SCHEDULE_EXACT_ALARM / USE_EXACT_ALARM:
         reminders use inexact allowWhileIdle + app-open reconcile (decision 3),
         dodging the Play-policy-restricted exact-alarm permissions. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- image_picker delegates to the system camera app via intent; declare the
         feature as optional (required=false) so camera-less devices can still
         install (Play Store filter). No CAMERA uses-permission — the intent-based
         picker needs none, and declaring it would force a runtime prompt. -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <!-- Android 11+ package visibility: lets url_launcher open https URLs
         (e.g. the app store from the force-update gate). -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>
    <application
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:networkSecurityConfig="@xml/network_security_config">
        <!-- Route platform deep links through Flutter so go_router handles them. -->
        <meta-data
            android:name="flutter_deeplinking_enabled"
            android:value="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Deep linking via a custom scheme (works with no domain). Test:
                 adb shell am start -a android.intent.action.VIEW \
                   -d "jagasaku:///users/<id>" <applicationId>
                 The empty authority (`///`) maps the URI path straight to a
                 go_router location. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="jagasaku" />
            </intent-filter>

            <!-- Android App Links (verified https — opens with no chooser).
                 Needs /.well-known/assetlinks.json hosted on your domain;
                 uncomment and set android:host:
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="example.com" />
            </intent-filter>
            -->
        </activity>
        <!-- flutter_local_notifications scheduled + boot receivers (V3-M5).
             v22 does NOT bundle these in its library manifest (only
             POST_NOTIFICATIONS/VIBRATE), so the app must declare them for
             zonedSchedule to fire and to reschedule after reboot. -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
